# Find documents where the "age" field is greater than 30
db.collection.find({"age": {"$gt": 30}})

# Find documents where the "price" field is less than 1000
db.collection.find({"price": {"$lt": 1000}})

